

# Ajustes de texto nos cards

## Mudanças em `CircularBudgetCard.tsx`

1. **Linha 691**: Trocar `"Dias restantes"` por `"Dias restantes no mês"`
2. **Linha 507**: Trocar `~{Math.floor(daysUntilEmpty)}d` por `{Math.floor(daysUntilEmpty)} dias até o saldo acabar`
3. **Linha 506**: Trocar `>1 ano` por `Mais de 1 ano até o saldo acabar`
4. **Linha 503**: Trocar `Saldo esgotado` por manter como está (já é descritivo)

